# Sample Flask App

Simple sample flask app for testing a MySQL database connection.

## Usage

Install required python modules with:

    pip3 install -r requirements.txt

Run the app with:

    python3 app.py
